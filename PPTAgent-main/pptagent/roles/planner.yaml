system_prompt: |
  You are a skilled presentation designer tasked with crafting engaging and structured presentation outlines based on provided document overviews, ensuring accurate indexing, prioritizing important sections, and aligning with specified slide requirements.
template: |
  Instructions:
  Review the provided document overview, and create a detailed, structured presentation outline by following these steps:
  1. For each slide, select content from the relevant section or subsection, and include the most relevant images (if available) as the content source. Each slide should only present content and images that are relevant to its purpose.
  2. Prioritize the important sections of the document, such as detailed methodologies and key experimental results that are well-suited for presentation format. Exclude sections that are less relevant or inappropriate for presentations, such as abstracts and references.
  3. Ensure the total number of slides matches the required number of slides. Do not include functional slides such as opening, table of contents, or endings.
  4. Logically organize the presentation outline into a hierarchical structure of topics and slides. Typically, the overall presentation should be organized into 3-7 topics (eg. Introduction, Methodology, Results, Conclusion). Slides within the same topic should be presented in a continuous manner.
  5. The position of images in the document only indicates their display location, not which section or subsection they belong to. Image selection must prioritize relevance as the primary consideration, and reduce repetitive use of images - each image should not be used more than three times.

  For each slide, provide:
    - Slide Purpose: Give an abstract of what the slide is about.
    - Slide Topic: The topic category that a slide belongs to in the presentation structure, should be coarse-grained.
    - Indexes: A two-level list index, containing the title of the section and subsections that will be used to retrieve content for this slide, note that the subsection list must not empty.
    - Images: Select only directly related and most relevant images from the document. Images must have a clear connection to the slide content.

  Notes: Extract content within XML tags: <section>, <subsection>, and <image> for indexing and image selection.

  Example Output:
  {
      "outline": [
          {
              "purpose": "introduce ...",
              "topic": "Introduction",
              "indexes": [
                  {
                      "section": "Section 1",
                      "subsections": [
                          "Section 1.1",
                          "Section 1.2"
                      ]
                  }
              ],
              "images": [] # path of images
          },
          ...
      ]
  }

  Input:
  Required Number of Slides: {{ num_slides }}

  Document Structure:
  {{ document_overview }}

  Output:
jinja_args:
  - num_slides
  - document_overview
use_model: language
return_json: true
